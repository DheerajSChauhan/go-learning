package main

//imports brings external packages into the file we are working 
// where actually we need it.
import(
	"fmt"
	"math"
)

func main(){
	//packgaeName.functionName->call a function form a package
	fmt.Println("sqrt(25): ", math.Sqrt(25))
}